package vision;

public class LimelightConstants {

    // Angle offsets (based of how the camera's positioned)
    public static final double X_CAMERA_ANGLE_OFFSET = 0;
    public static final double Y_CAMERA_ANGLE_OFFSET = 63;

    // Offset from Camera's position relative to the robot's center.
    public static final double X_CENTER_OFFSET = 0;
    public static final double Y_CENTER_OFFSET = 0;
    public static final double Z_CENTER_OFFSET = 0;

    // Camera's height realtive to the robot's bottom, and Target's height.
    public static final double CAMERA_HEIGHT = 0.60;
    public static final double TARGET_HEIGHT = 2.59;

    public static final double TARGET_NOT_VISIBLE = -999;

}
